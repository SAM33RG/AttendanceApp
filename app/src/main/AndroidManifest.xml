<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.attendanceapp.attendanceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddClassActivity" />
        <activity android:name=".AddStudentsActivity" />
        <activity android:name=".AddStudentDataActivity" />
        <activity android:name=".TakeAttendanceAcitivity" />
        <activity android:name=".AddLecture" />
        <activity android:name=".TakeStudentAttendanceActivity" />
        <activity android:name=".ViewAttendanceShowClasses"></activity>
    </application>

</manifest>